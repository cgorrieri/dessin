<%= form_for(@forum_part) do |f| %>
  <% if @forum_part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forum_part.errors.count, "error") %> prohibited this forum_part from being saved:</h2>

      <ul>
      <% @forum_part.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
